<!-- start location -->
        <div class="mt-6">
          <div class="px-4 xl:px-8">
            <h3 class="text-gray-900 text-xl">
              <%= "#{location.name}"%>
                
            </h3>
            <p class="text-gray-600"><%= "#{location.description}"%></p>
          </div>
          <div class="mt-6 sm:overflow-x-auto sm:overflow-y-hidden">

          <div class="px-4 sm:inline-flex sm:pt-2 sm:pb-8 xl:px-8">

          <!--  -->

          <div class="sm:mt-0 sm:w-80 sm:flex-shrink-0 mt-10 sm:ml-4">
                      <%= render partial: 'property_card', collection: location.listings %>
          <div>
        </div>
      </div>
    </div>
  </div>
</div>
<!-- finish location -->
<!--  -->
        <div class="mt-6">
          <div class="px-4 xl:px-8">
            <h3 class="text-gray-900 text-xl"><%= "#{location.name}"%></h3>
            <p class="text-gray-600"><%= "#{location.description}"%></p>
          </div>
          <div class="mt-6 sm:overflow-x-auto sm:overflow-y-hidden">
            <div class="px-4 sm:inline-flex sm:pt-2 sm:pb-8 xl:px-8">
              <div v-for="(property, i) in location.properties" :class="{'mt-10 sm:ml-4': i > 0 }" class="sm:mt-0 sm:w-80 sm:flex-shrink-0">
                <%= render partial: 'property_card', collection: location.listings %>
              </div>
            </div>
          </div>
        </div>
